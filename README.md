# homebrew-deepseek-tui

Homebrew tap for [DeepSeek TUI](https://github.com/Hmbown/DeepSeek-TUI) — a terminal-native coding agent for DeepSeek V4.

## Install

```bash
brew tap coohu/deepseek-tui
brew install deepseek-tui
```

This installs both binaries: `deepseek` (dispatcher CLI) and `deepseek-tui` (TUI).

## Update

```bash
brew upgrade deepseek-tui
```
